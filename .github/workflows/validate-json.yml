name: JSON check

on:
  push:
    paths:
      - '**.json'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: json-syntax-check
        uses: bigfishgames/json-syntax-check@v1.0.0
        with:
          pattern: "\\.json$"